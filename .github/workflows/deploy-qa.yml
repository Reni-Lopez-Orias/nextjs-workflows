name: Deploy - QA (Vercel preview)

on:
  push:
    branches: [qa]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      vercel-environment: preview
      extra-flag: ''
    secrets: inherit
