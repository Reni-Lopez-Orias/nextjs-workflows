name: Deploy - Prod (Vercel production)

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      vercel-environment: production
      extra-flag: '--prod'
    secrets: inherit
